{
    "mcpServers": {
      "figma": {
        "command": "npx",
        "args": ["@figma/mcp-server-figma"],
        "env": {
          "FIGMA_ACCESS_TOKEN": "${FIGMA_API_KEY}"
        }
      }
    }
  }